// const { add, subtract, multiply } = require("./cjs_math.js");

const greet = (name) => {
  // console.log(add(1, 2));
  console.log(`Hello, ${name}!`);
};

// greet("World");

module.exports = greet;
